# Examples

All files under `examples/` are **synthetic demonstrations only**.

- `sample_data/` contains fictional patient IDs (`P001`, etc.) — not real claims data
- YAML definitions (including `fpies_feature_definitions.yaml`) are **sample configurations**
- Users must validate clinical and operational appropriateness before research use
- No proprietary code masters or real aggregate statistics are included
